INSERT INTO threat (
    binding_id,
    artifact_digest,
    threat_key,
    threat_id,
    model,
    subject_id,
    product,
    statement,
    surface,
    asset,
    impact,
    likelihood,
    status,
    controls,
    actors,
    evidence,
    linddun,
    score,
    isolation_dimensions,
    isolation_boundaries
)
VALUES (
    :binding_id,
    :artifact_digest,
    :threat_key,
    :threat_id,
    :model,
    :subject_id,
    :product,
    :statement,
    :surface,
    :asset,
    :impact,
    :likelihood,
    :status,
    :controls,
    :actors,
    :evidence,
    :linddun,
    :score,
    :isolation_dimensions,
    :isolation_boundaries
)
ON CONFLICT (binding_id, threat_key) DO NOTHING;
