{
  "name": "threat-impact",
  "source_schema": "threat-register.schema.json#/$defs/threat/properties/impact",
  "description": "How bad this threat is if realised, modelled BEFORE reachability is known. Deliberately not the finding severity enum.",
  "values": [
    "low",
    "medium",
    "high",
    "critical",
    "existential"
  ],
  "note": "Has no overlap with severity by design: a threat model reasons about consequence, a finding about a confirmed defect. \"existential\" has no severity counterpart and must not be flattened onto \"critical\".",
  "used_in_schemas": [
    "threat-register.schema.json"
  ]
}
