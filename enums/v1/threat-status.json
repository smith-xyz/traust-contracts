{
  "name": "threat-status",
  "source_schema": "threat-register.schema.json#/$defs/threat/properties/status",
  "description": "Mitigation state of a modelled threat.",
  "values": [
    "unmitigated",
    "partially_mitigated",
    "mitigated",
    "risk_accepted"
  ],
  "note": "\"partially_mitigated\" is a distinct state and the LARGEST bucket in practice, so folding it into \"mitigated\" overstates coverage more than any other collapse here. \"risk_accepted\" is an affirmative decision, not an absence of one.",
  "used_in_schemas": [
    "threat-register.schema.json"
  ]
}
